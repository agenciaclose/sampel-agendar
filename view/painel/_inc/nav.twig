<!-- BEGIN: Header-->
    <nav class="header-navbar navbar navbar-expand-lg align-items-center floating-nav navbar-light navbar-shadow">
        <div class="navbar-container d-flex content">
            <div class="bookmark-wrapper d-flex align-items-center">
                <ul class="nav navbar-nav d-xl-none">
                    <li class="nav-item"><a class="nav-link menu-toggle" href="#"><i class="ficon" data-feather="menu"></i></a></li>
                </ul>
                <ul class="nav navbar-nav">
                    <li class="nav-item d-none d-lg-block"><a class="nav-link nav-link-style"><i class="ficon" data-feather="{{ (_cookie.siteStyle == 'light-layout') ? 'moon' : 'sun' }}"></i></a></li>
                </ul>
            </div>
            <ul class="nav navbar-nav align-items-center ms-auto">
                 <li class="nav-item dropdown dropdown-user">
                    <a class="nav-link dropdown-toggle dropdown-user-link" id="dropdown-user" href="#" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <div class="user-nav d-sm-flex d-none">
                            <span class="user-name fw-bolder">{{ _session.sampel_user_nome }}</span>
                            {% if _session.sampel_user_status == 1 %}
                                <span class="user-status text-success text-capitalize">Ativo</span>
                            {% elseif _session.sampel_user_status == 2  %}
                                <span class="user-status text-warning text-capitalize">Pendente</span>
                            {% else %}
                                <span class="user-status text-danger text-capitalize">Inativo</span>
                            {% endif %}                            
                        </div>
                        <span class="avatar">
                            {% if(_session.sampel_user_imagem) != '' %}
                                <img class="round" src="{{ PATH }}/uploads/{{ _session.sampel_user_imagem }}" alt="avatar" height="40" width="40">
                            {% else %}
                                <i class="fa-thin fa-circle-user fa-3x"></i>
                            {% endif %}
                            {% if _session.sampel_user_status == 1 %}
                               <span class="avatar-status-online"></span>
                            {% elseif _session.sampel_user_status == 2  %}
                               <span class="avatar-status-away"></span>
                            {% else %}
                               <span class="avatar-status-busy"></span>
                            {% endif %}                            
                        </span>
                    </a>
                    <div class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdown-user">
                        <a class="dropdown-item" href="{{ DOMAIN }}/painel/curriculum"><i class="me-50" data-feather="user"></i> Meu Perfil</a>
                        <a class="dropdown-item" href="{{ DOMAIN }}/painel/curriculum"><i class="me-50" data-feather="user"></i> Editar Perfil</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ DOMAIN }}/painel/login/logout"><i class="me-50" data-feather="power"></i> Sair</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>