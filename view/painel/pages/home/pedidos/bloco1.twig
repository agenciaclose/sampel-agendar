<div class="row g-6 mb-2">

    <div class="col-lg-3 col-sm-6">
        <div class="card card-border-shadow-info h-100">
            <div class="card-body pb-0">
                <div class="d-flex align-items-center mb-2">
                    <div class="avatar me-1">
                        <span class="avatar-initial rounded bg-label-info">
                            <i class="fa-regular fs-5 fa-cart-circle-plus"></i>
                        </span>
                    </div>
                    <h2 class="mb-0">{{ pedidosTotal|length }}</h2>
                </div>
                <h4 class="mb-1"><a href="{{ DOMAIN }}/painel/pedidos">Total de Pedidos</a></h4>
                <p class="mb-0">
                    <span class="text-heading fw-bolder text-info me-2">R$ {{ PedidosValorTotal.valor_total_pedido|number_format(2, ',', '.') }}</span>
                    <small>Valor Total de todos os pedidos</small>
                </p>
            </div>
        </div>
    </div>

    <div class="col-lg-3 col-sm-6">
        <div class="card card-border-shadow-success h-100">
            <div class="card-body pb-0">
                <div class="d-flex align-items-center mb-2">
                    <div class="avatar me-1">
                        <span class="avatar-initial rounded bg-label-success">
                            <i class="fa-regular fs-5 fa-box-open-full"></i>
                        </span>
                    </div>
                    <h2 class="mb-0">{{ produtosSemPDV|length }}</h2>
                </div>
                <h4 class="mb-1"><a href="{{ DOMAIN }}/painel/produtos">Total de Produtos</a></h4>
                <p class="mb-0">
                    <span class="text-heading fw-bolder text-success me-2">R$ {{ valorTotalEstoque.valor_total_estoque|number_format(2, ',', '.') }}</span>
                    <small>Valor Estoque</small>
                </p>
            </div>
        </div>
    </div>

    <div class="col-lg-3 col-sm-6">
        <div class="card card-border-shadow-warning h-100">
            <div class="card-body pb-0">
                <div class="d-flex align-items-center mb-2">
                    <div class="avatar me-1">
                        <span class="avatar-initial rounded bg-label-warning">
                            <i class="fa-regular fs-5 fa-recycle"></i>
                        </span>
                    </div>
                    <h2 class="mb-0">{{ produtosPDV|length }}</h2>
                </div>
                <h4 class="mb-1"><a href="{{ DOMAIN }}/painel/produtos?tipo=PDV">Total de Produtos PDV</a></h4>
                <p class="mb-0">
                    <span class="text-heading fw-bolder text-warning me-2">R$ {{ valorTotalEstoquePDV.valor_total_estoque_PDV|number_format(2, ',', '.') }}</span>
                    <small>Valor Estoque PDV</small>
                </p>
            </div>
        </div>
    </div>


    <div class="col-lg-3 col-sm-6">
        <div class="card card-border-shadow-danger h-100">
            <div class="card-body pb-0">
                <div class="d-flex align-items-center mb-2">
                    <div class="avatar me-1">
                        <span class="avatar-initial rounded bg-label-danger">
                            <i class="ti ti-clock ti-28px"></i>
                            <i class="fa-regular fs-5 fa-cart-circle-xmark"></i>
                        </span>
                    </div>
                    <h2 class="mb-0">{{ produtosEstoqueBaixo|length }}</h2>
                </div>
                <h4 class="mb-1"><a href="{{ DOMAIN }}/painel/produtos?tipo=estoque">Estoque Baixo</a></h4>
                <p class="mb-0">
                    <span class="text-heading fw-bolder text-danger me-2">R$ {{ valorTotalEstoqueBaixo.total_reabastecimento|number_format(2, ',', '.') }}</span>
                    <small>Valor Total para Reabastecer o Estoque</small>
                </p>
            </div>
        </div>
    </div>

</div>